# --- runtime ---
faker                 # demo dataset generation only (demo_dataset/generate_data.py)
fastapi
uvicorn[standard]
httpx                 # UiPath Data Service + Context Grounding HTTP calls
pydantic>=2
pydantic-settings>=2

# --- UiPath coded agent (deploy to Automation Cloud) ---
uipath                # UiPath Python SDK + CLI (uipath init/pack/publish)

# --- test ---
pytest
pytest-asyncio
respx                 # mock HTTP (UiPath clients + OFAC SDN fetch)
coverage
