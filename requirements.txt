# --- runtime ---
faker
pymongo
google-genai          # current Vertex AI GenAI SDK (embeddings: gemini-embedding-001)
mcp                   # MCP client SDK — runtime reads via the MongoDB MCP server
fastapi
uvicorn[standard]
pydantic>=2
pydantic-settings>=2

# --- test ---
pytest
pytest-asyncio
mongomock             # in-memory MongoDB for tests (no Atlas needed)
respx                 # mock HTTP (OFAC SDN fetch)
httpx
coverage
